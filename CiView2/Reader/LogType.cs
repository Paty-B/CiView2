namespace CiView.Recorder.Reader
{
    public enum LogType
    {
        OnUnfilteredLog             = 0,

        OnOpenGroup                 = 1,

        OnOpenGroupWithException    = 2,

        OnGroupClosed               = 3
    }
}
